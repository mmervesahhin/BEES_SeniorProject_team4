 import 'package:flutter/material.dart';

class ItemReportsScreen extends StatelessWidget {
  const ItemReportsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Item Reports'),
      ),
      body: Center(
        child: Text('Item Reports Screen'),
      ),
    );
  }
}